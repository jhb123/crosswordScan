[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
requires-python = ">=3.9"


dependencies = [
	"cws>=0.0.1",
    "fastapi==0.110.0",
    "uvicorn==0.28.1",
    "Jinja2==3.1.3"
]
